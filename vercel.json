{
    "builds": [
        {
            "src": "library_1/wsgi.py",
            "use": "@vercel/python",
            "config": {
                "maxLambdaSize": "15mb",
                "runtime": "python3.9",
                "buildCommand": "export MYSQLCLIENT_CFLAGS=\"$(mysql_config --cflags)\" && export MYSQLCLIENT_LDFLAGS=\"$(mysql_config --libs)\" && pip install -r requirements.txt"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "library_1/wsgi.py"
        }
    ]
}

